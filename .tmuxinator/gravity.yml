name: gravity
root: .

windows:
  - servers:
      layout: tiled
      panes:
        - server:
          - yarn
          - yarn workspace @gravityci/server start:dev
        - tunnel:
          - cloudflared tunnel --url http://localhost:3000
        -
