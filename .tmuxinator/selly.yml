name: jekyll
root: .

windows:
  - servers:
      layout: tiled
      panes:
        - claude:
          - claude
        - dev_server:
          - pnpm i --frozen-lockfile
          - pnpm dev
        -
