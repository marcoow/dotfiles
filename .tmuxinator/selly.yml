name: jekyll
root: .

windows:
  - servers:
      layout: tiled
      panes:
        - dev_server:
          - tmux resize-pane -U 5
          - pnpm i --frozen-lockfile
          - pnpm dev
        - tunnel:
          - cloudflared tunnel --url http://localhost:3000
        - containers:
          - podman compose up
        - claude:
          - opencode
        -
