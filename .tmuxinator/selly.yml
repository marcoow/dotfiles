name: jekyll
root: .

windows:
  - servers:
      layout: tiled
      panes:
        - codex:
          - codex
        - dev_server:
          - pnpm i --frozen-lockfile
          - pnpm dev
        -
